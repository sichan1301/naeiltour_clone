type textLinkType ={
  text:string,
  link:string
}

export type productType = {
    text:string,
    link:string
    type?:string
  }

export type menuType = {
  title:string,
  link:string,
  menuCategory:textLinkType[]
}

export type subMenuType = {
  
}